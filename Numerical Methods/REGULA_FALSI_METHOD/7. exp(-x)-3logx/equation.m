function out=equation(x)
	%out=x.*sin(x)+cos(x);% change this when the function is changed
    %out=x-sin(x)-0.5;
    %out=cos(x)-x.*exp(x);% change this when the function is changed
    %out=power(x,4)-x-10;
    out=exp(-3)-3*log(x);
    %out=x-exp(-x);
 end 
